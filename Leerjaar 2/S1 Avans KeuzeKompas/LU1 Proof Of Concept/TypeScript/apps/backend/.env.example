# Example .env file (copy to .env and fill values). Do not commit this file with real secrets.

# App
NODE_ENV=   # development | production | test
PORT=       # e.g. 3000

# Logging & monitoring
LOG_LEVEL=  # trace | debug | info | warn | error | fatal

# Database (MongoDB)
# Examples:
# - localhost without auth: mongodb://localhost:27017/myapp
# - with user/pass:     mongodb://user:pass@localhost:27017/myapp?authSource=admin
DATABASE_URL=mongodb://localhost:27017/myapp

# Authentication / JWT
# Use a long, random secret in production (e.g. 32+ chars)
JWT_SECRET=your_jwt_secret_here

# CORS / Frontend
CORS_ORIGIN=http://localhost:4200   # can be comma-separated list or * (use cautiously)