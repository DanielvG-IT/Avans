<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{{title}}} - Eject!</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/avans-logo.png" />

    <meta name="theme-color" content="#141414" />

    <style>
        body {
            background-color: #141414;
            color: #f1f1f1;
            font-family: "Inter", "Segoe UI", Roboto, sans-serif;
            line-height: 1.6;
        }

        .navbar,
        .dropdown-menu {
            background-color: #141414 !important;
            border-bottom: 1px solid #2a2a2a;
        }

        .nav-link {
            color: #b3b3b3 !important;
            font-weight: 500;
            transition: color 0.2s ease;
        }

        .nav-link:hover {
            color: #fff !important;
        }

        main {
            padding: 4rem 1rem;
            min-height: calc(100vh - 160px);
        }

        footer {
            background-color: #0f0f0f;
            border-top: 1px solid #2a2a2a;
            padding: 1.5rem 0;
            text-align: center;
            font-size: 0.9rem;
            color: #888;
        }

        footer a {
            color: #b3b3b3;
            text-decoration: none;
        }

        footer a:hover {
            color: #fff;
        }

        /* Subtle card style for body content */
        .content-card {
            background-color: #1c1c1c;
        }
    </style>
</head>

<body>
    {{> header}}

    <!-- Main Content -->
    <main class="container-fluid mx-auto mt-4 content-card">
        {{{body}}}
    </main>

    <!-- Footer -->
    {{> footer}}

    <!-- Bootstrap JS -->
    <script src=" https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js">
    </script>
    <script>
        async function clientLogOut() {
            try {
                const response = await fetch('/auth/logout', { method: 'DELETE', credentials: 'include' });
                if (response.status === 204) window.location.href = "/";
                else alert('Logout failed. Try again.');
            } catch (err) {
                alert('Logout failed. Try again.');
            }
        }
    </script>
</body>

</html>